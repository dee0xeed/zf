: do-impl r> rot rot swap >r >r >r ;
: var create 1 allot ;
: ? @ . cr ;
: r@ r> dup >r ;
: i r> r> dup >r swap >r ;
: +! dup @ rot + swap ! ;
: 1+ 1 + ;
: 1- 1 - ;
: max dup rot dup rot > if swap drop else drop then ;
: min dup rot dup rot < if swap drop else drop then ;
: 0= 0 = if 1 else 0 then ;
: 0<> 0 <> if 1 else 0 then ;
: 0> 0 > if 1 else 0 then ;
: 0< 0 < if 1 else 0 then ;
